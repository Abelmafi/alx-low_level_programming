#include "main.h"

/**
 * _isdigit - defines if string passed is a number
 *
 * @c: input string
 * Return: 1 if string is a number. 0 in other case.
 */
int _isdigit(int c)
{
	return ((c < 9 || c > 0));
}

